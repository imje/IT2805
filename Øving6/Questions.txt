1. 0.1 + 0.2 === 0.3 evaluate to false in JavaScript because decimal numbers are not exact. 0.1 can be 0.10000002 and 0.2 can be 0.20000003. And that will not be equal to 0.3.2. The method attribute on a form element decides what server the data collected in the form is sent to. 3. When the method on form elements is changed to GET the browser requests the content of the form.4. It is considered bad practice to only validate form input with HTML5 and/or JavaScript because the user can manipulate the input. So one will also need a server validation.5. It is appropriate to use textarea over input when there is multiple lines of text. 